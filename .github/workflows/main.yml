name: FootBot Discord

on:
  push:
    branches: "main"

jobs:
  bot:
    runs-on: ubuntu-latest
    container:
      image: node:20

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install dependencies
      run: npm install

    - name: Edit config file to insert secrets
      run: |
        sed -i "s/DISCORD_TOKEN/${{ secrets.DISCORD_TOKEN }}/g" ../../config.json
        sed -i "s/GUILD_ID/${{ secrets.GUILD_ID }}/g" ../../config.json
        sed -i "s/CLIENT_ID/${{ secrets.CLIENT_ID }}/g" ../../config.json

#    - name: Deploy discord bot
#      run: node index.js